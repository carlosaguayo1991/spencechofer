<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pullmandelnorte.spencechofer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_spence"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/icono_spence"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.pullmandelnorte.spencechofer.FormularioChoferesActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.EncuestaChoferes"
            android:configChanges="screenSize|orientation"
            android:label="@string/title_activity_encuesta_choferes"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name="com.pullmandelnorte.spencechofer.FormularioPreguntasActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.IntermedioActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.BuscarViaje"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.EscaneoActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.RegistrarUsuario"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.pullmandelnorte.spencechofer.Login"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.pullmandelnorte.spencechofer.MainActivity"
            android:configChanges="screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.wearable.standalone" -->
        <!-- android:value="true" /> -->
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.pullmandelnorte.spencechofer.entities" />
        <meta-data
            android:name="DATABASE"
            android:value="spencechofer.db" />
        <meta-data
            android:name="VERSION"
            android:value="5" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
    </application>

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>